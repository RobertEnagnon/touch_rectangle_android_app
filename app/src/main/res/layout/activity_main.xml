<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingTop="16dp"
    android:paddingRight="16dp"
    android:paddingBottom="16dp">

    <TextView
        android:id="@+id/size_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:textSize="18sp"
        android:textColor="#fff"
        tools:text="Size: 0.00%" />

    <TextView
        android:id="@+id/length_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/size_id"
        android:textColor="#fff"
        android:textSize="18sp"
        tools:text="Length: 0 cm" />

    <TextView
        android:id="@+id/circumference_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/length_id"
        android:textColor="#fff"
        android:textSize="18sp"
        tools:text="Circumference: 0 cm" />

    <TextView
        android:id="@+id/area_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/circumference_id"
        android:textSize="18sp"
        android:textColor="#fff"
        tools:text="Area: 0 cm²" />

    <View
        android:id="@+id/rectangleView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#ff0000"
        android:alpha="0.3"
        android:visibility="invisible" />

</RelativeLayout>
